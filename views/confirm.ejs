<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/confirm.css">
    <title>Confirm Booking</title>
</head>
<body>
    <h2>Booking Confirmation</h2>

    <div style="text-align: center;">
        <p><strong>User Type:</strong> <%= booking.user_type %></p>
        <p><strong>Section:</strong> <%= booking.section %></p>
        <p><strong>Subject:</strong> <%= booking.subject %></p>
        <p><strong>Purpose:</strong> <%= booking.purpose %></p>
        <p><strong>Lab:</strong> <%= booking.lab %></p>
        <p><strong>Booking Date:</strong> <%= booking.booking_date %></p>
        <p><strong>Start Time:</strong> <%= booking.start_time %></p>
        <p><strong>End Time:</strong> <%= booking.end_time %></p>
    </div>

    <form action="/lab" method="POST">
        <input type="hidden" name="user_type" value="<%= booking.user_type %>">
        <input type="hidden" name="section" value="<%= booking.section %>">
        <input type="hidden" name="subject" value="<%= booking.subject %>">
        <input type="hidden" name="purpose" value="<%= booking.purpose %>">
        <input type="hidden" name="lab" value="<%= booking.lab %>">
        <input type="hidden" name="booking_date" value="<%= booking.booking_date %>">
        <input type="hidden" name="start_time" value="<%= booking.start_time %>">
        <input type="hidden" name="end_time" value="<%= booking.end_time %>">
        <button type="submit">Confirm Booking</button>
    </form>

    <a href="/lab">Cancel</a>
</body>
</html>
